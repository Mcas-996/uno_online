[package]
name = "uno"
edition = "2024"
license = "AGPL-3.0-only"
repository = "https://github.com/Mcas-996/uno_online"
version = "0.5.3-developer-beta-1"

[dependencies]
crossterm = "0.28.1"
rand = "0.8.5"
ratatui = "0.29.0"
sys-locale = "0.3.2"

[lints.rust]
unsafe_code = "forbid"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
